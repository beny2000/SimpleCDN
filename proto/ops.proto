syntax = "proto3";

service FileServer {
  rpc put(stream Chunk) returns (Reply) {}
  rpc get(Request) returns (stream Chunk) {}
}

message Chunk {
  bytes buffer = 1;
}

message Request {
  string name = 1;
}

message Reply {
  int32 length = 1;
}